<?xml version="1.0" encoding="utf-8"?>

<StackLayout xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Material_TabView.TabView.TabView"
             x:Name="TabViewContext" MinimumHeightRequest="50"
             Spacing="0">
    
    <ContentView x:Name="ShownTabContent" VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand" />
    
    <Grid x:Name="TabContainer" 
          HorizontalOptions="Fill"
          HeightRequest="{Binding TabContainerHeight, Source={x:Reference TabViewContext}}"
          ColumnSpacing="0"
          BackgroundColor="{Binding TabContainerBackgroundColor, Source={x:Reference TabViewContext}}"
           />
    
</StackLayout>